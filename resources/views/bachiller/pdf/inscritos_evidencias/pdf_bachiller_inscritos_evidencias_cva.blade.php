<!DOCTYPE html>
<html>

<head>
    <title>Calificaciones Evidencias</title>
    <!-- <link rel="stylesheet" href="sass/main.css" media="screen" charset="utf-8"/> -->
    <meta content="width=device-width, initial-scale=1.0" name="viewport">
    <meta http-equiv="content-type" content="text-html; charset=utf-8">
    <style type="text/css">
        /*! normalize.css v8.0.1 | MIT License | github.com/necolas/normalize.css */
        /* Document
        ========================================================================== */
        /**
      * 1. Correct the line height in all browsers.
      * 2. Prevent adjustments of font size after orientation changes in iOS.
      */
        html {
            line-height: 1.15;
            /* 1 */
            -webkit-text-size-adjust: 100%;
            /* 2 */
        }

        /* Sections
        ========================================================================== */
        /**
      * Remove the margin in all browsers.
      */
        body {
            margin: 0;
        }

        /**
      * Render the `main` element consistently in IE.
      */
        main {
            display: block;
        }

        /**
      * Correct the font size and margin on `h1` elements within `section` and
      * `article` contexts in Chrome, Firefox, and Safari.
      */
        h1 {
            font-size: 2em;
            margin: 0.67em 0;
        }

        /* Grouping content
        ========================================================================== */
        /**
      * 1. Add the correct box sizing in Firefox.
      * 2. Show the overflow in Edge and IE.
      */
        hr {
            box-sizing: content-box;
            /* 1 */
            height: 0;
            /* 1 */
            overflow: visible;
            /* 2 */
        }

        /**
      * 1. Correct the inheritance and scaling of font size in all browsers.
      * 2. Correct the odd `em` font sizing in all browsers.
      */
        pre {
            font-family: monospace, monospace;
            /* 1 */
            font-size: 1em;
            /* 2 */
        }

        /* Text-level semantics
        ========================================================================== */
        /**
      * Remove the gray background on active links in IE 10.
      */
        a {
            background-color: transparent;
        }

        /**
      * 1. Remove the bottom border in Chrome 57-
      * 2. Add the correct text decoration in Chrome, Edge, IE, Opera, and Safari.
      */
        abbr[title] {
            border-bottom: none;
            /* 1 */
            text-decoration: underline;
            /* 2 */
            text-decoration: underline dotted;
            /* 2 */
        }

        /**
      * Add the correct font weight in Chrome, Edge, and Safari.
      */
        b,
        strong {
            font-weight: bolder;
        }

        /**
      * 1. Correct the inheritance and scaling of font size in all browsers.
      * 2. Correct the odd `em` font sizing in all browsers.
      */
        code,
        kbd,
        samp {
            font-family: monospace, monospace;
            /* 1 */
            font-size: 1em;
            /* 2 */
        }

        /**
      * Add the correct font size in all browsers.
      */
        small {
            font-size: 80%;
        }

        /**
      * Prevent `sub` and `sup` elements from affecting the line height in
      * all browsers.
      */
        sub,
        sup {
            font-size: 75%;
            line-height: 0;
            position: relative;
            vertical-align: baseline;
        }

        sub {
            bottom: -0.25em;
        }

        sup {
            top: -0.5em;
        }

        /* Embedded content
        ========================================================================== */
        /**
      * Remove the border on images inside links in IE 10.
      */
        img {
            border-style: none;
        }

        /* Forms
        ========================================================================== */
        /**
      * 1. Change the font styles in all browsers.
      * 2. Remove the margin in Firefox and Safari.
      */
        button,
        input,
        optgroup,
        select,
        textarea {
            font-family: inherit;
            /* 1 */
            font-size: 100%;
            /* 1 */
            line-height: 1.15;
            /* 1 */
            margin: 0;
            /* 2 */
        }

        /**
      * Show the overflow in IE.
      * 1. Show the overflow in Edge.
      */
        button,
        input {
            /* 1 */
            overflow: visible;
        }

        /**
      * Remove the inheritance of text transform in Edge, Firefox, and IE.
      * 1. Remove the inheritance of text transform in Firefox.
      */
        button,
        select {
            /* 1 */
            text-transform: none;
        }

        /**
      * Correct the inability to style clickable types in iOS and Safari.
      */
        button,
        [type="button"],
        [type="reset"],
        [type="submit"] {
            -webkit-appearance: button;
        }

        /**
      * Remove the inner border and padding in Firefox.
      */
        button::-moz-focus-inner,
        [type="button"]::-moz-focus-inner,
        [type="reset"]::-moz-focus-inner,
        [type="submit"]::-moz-focus-inner {
            border-style: none;
            padding: 0;
        }

        /**
      * Restore the focus styles unset by the previous rule.
      */
        button:-moz-focusring,
        [type="button"]:-moz-focusring,
        [type="reset"]:-moz-focusring,
        [type="submit"]:-moz-focusring {
            outline: 1px dotted ButtonText;
        }

        /**
      * Correct the padding in Firefox.
      */
        fieldset {
            padding: 0.35em 0.75em 0.625em;
        }

        /**
      * 1. Correct the text wrapping in Edge and IE.
      * 2. Correct the color inheritance from `fieldset` elements in IE.
      * 3. Remove the padding so developers are not caught out when they zero out
      *    `fieldset` elements in all browsers.
      */
        legend {
            box-sizing: border-box;
            /* 1 */
            color: inherit;
            /* 2 */
            display: table;
            /* 1 */
            max-width: 100%;
            /* 1 */
            padding: 0;
            /* 3 */
            white-space: normal;
            /* 1 */
        }

        /**
      * Add the correct vertical alignment in Chrome, Firefox, and Opera.
      */
        progress {
            vertical-align: baseline;
        }

        /**
      * Remove the default vertical scrollbar in IE 10+.
      */
        textarea {
            overflow: auto;
        }

        /**
      * 1. Add the correct box sizing in IE 10.
      * 2. Remove the padding in IE 10.
      */
        [type="checkbox"],
        [type="radio"] {
            box-sizing: border-box;
            /* 1 */
            padding: 0;
            /* 2 */
        }

        /**
      * Correct the cursor style of increment and decrement buttons in Chrome.
      */
        [type="number"]::-webkit-inner-spin-button,
        [type="number"]::-webkit-outer-spin-button {
            height: auto;
        }

        /**
      * 1. Correct the odd appearance in Chrome and Safari.
      * 2. Correct the outline style in Safari.
      */
        [type="search"] {
            -webkit-appearance: textfield;
            /* 1 */
            outline-offset: -2px;
            /* 2 */
        }

        /**
      * Remove the inner padding in Chrome and Safari on macOS.
      */
        [type="search"]::-webkit-search-decoration {
            -webkit-appearance: none;
        }

        /**
      * 1. Correct the inability to style clickable types in iOS and Safari.
      * 2. Change font properties to `inherit` in Safari.
      */
        ::-webkit-file-upload-button {
            -webkit-appearance: button;
            /* 1 */
            font: inherit;
            /* 2 */
        }

        /* Interactive
        ========================================================================== */
        /*
      * Add the correct display in Edge, IE 10+, and Firefox.
      */
        details {
            display: block;
        }

        /*
      * Add the correct display in all browsers.
      */
        summary {
            display: list-item;
        }

        /* Misc
        ========================================================================== */
        /**
      * Add the correct display in IE 10+.
      */
        template {
            display: none;
        }

        /**
      * Add the correct display in IE 10.
      */
        [hidden] {
            display: none;
        }

        body {
            font-family: 'sans-serif';
            font-size: 11px;
            margin-top: 155px;
            margin-left: 5px;
            margin-right: 5px;
        }

        .row {
            width: 100%;
            display: block;
            position: relative;
            margin-left: -30px;
            margin-right: -30px;
        }

        .row::after {
            content: "";
            clear: both;
            display: table;
        }

        .column,
        .columns {
            width: 100%;
            float: left;
            box-sizing: border-box !important;
        }

        .medium-1 {
            width: 8.33333333333%;
        }

        .medium-4 {
            width: 16.6666666667%;
        }

        .medium-3 {
            width: 25%;
        }

        .medium-4 {
            width: 33.3333333333%;
        }

        .medium-5 {
            width: 41.6666666667%;
        }

        .medium-6 {
            width: 50%;
        }

        .medium-7 {
            width: 58.3333333333%;
        }

        .medium-8 {
            width: 66.6666666667%;
        }

        .medium-9 {
            width: 75%;
        }

        .medium-10 {
            width: 83.3333333333%;
        }

        .medium-11 {
            width: 91.6666666667%;
        }

        .medium-12 {
            width: 100%;
        }

        .clearfix::after {
            content: "";
            clear: both;
            display: table;
        }

        span {
            font-weight: bold;
        }

        p {
            margin: 0;
        }

        .left {
            float: left;
        }

        .float-right: {
            float: right;
        }

        .logo {
            width: 100%;
        }

        .box-solicitud {
            border: 1px solid "black";
            padding: 5px;
            border-radius: 2px;
        }

        .estilos-tabla {
            width: 100%;
        }

        .estilos-tabla tr th {
            font-size: 12px;
            background-color: #000;
            color: #fff;
            height: 30px;
            padding-left: 5px;
            padding-right: 5px;
            box-sizing: border-box;
            text-align: center;
        }

        .estilos-tabla tr td {
            font-size: 12px;
            padding-left: 2px;
            padding-right: 2px;
            box-sizing: border-box;
            color: #000;
        }

        .page_break {
            page-break-before: always;
        }

        /** Define the footer rules **/
        footer {
            position: fixed;
            bottom: 0px;
            left: 0cm;
            right: 0cm;
            /** Extra personal styles **/
            color: #000;
            text-align: center;
        }

        header {
            position: fixed;
            top: 7px;
            right: 0px;
            height: 3px;
            /** Extra personal styles **/

            margin-left: 5px;
            margin-right: 5px;
        }

        #watermark {
            position: fixed;
            top: 15%;
            left: 0;
            width: 700px;
            height: 700px;
            opacity: .3;
        }

        .img-header {
            height: 80px;
        }

        .inicio-pagina {
            margin-top: 0;
            display: block;
        }

        @page {
            margin-top: 30px;
            margin-bottom: 70px;
        }

        .listas-info {
            margin-top: 0px;
            margin-bottom: 0px;
        }

        .listas-info li {
            display: inline;
            list-style-type: none;
            margin-left: 40px;

        }

        .listas-info li:first-child {
            margin-left: 0px;
        }

        .listas-asistencia {
            margin-top: 0px;
            margin-bottom: 0px;
            margin-left: 0px !important;
            padding-left: 0 !important;
        }

        .listas-asistencia li {
            display: inline;
            list-style-type: none;
        }

        .table {
            width: 100%;
        }

        .table {
            border-collapse: collapse;
        }

        .table th {
            border-bottom: 1px solid #000;
        }

        .table td,
        .table th {
            padding-top: 0px;
            padding-bottom: 0px;
            padding-right: 5px;
        }

        .page-number:before {
            content: "Pág "counter(page);
        }

        .punteado2 {
            border-top: 1px dotted;
            border-right: 1px dotted;
            border-bottom: 1px dotted;
            border-left: 1px dotted;
        }

        .punteado {
            border-top: 1px dotted;
            border-right: 1px dotted;
            border-bottom: 1px;
            border-left: 1px dotted;
        }

        .punteado3 {
            border-top: 1px;
            border-right: 0px dotted;
            border-bottom: 1px dotted;
            border-left: 1px dotted;
        }
    </style>
</head>

<body>

    @php
        
        use App\Http\Helpers\Utils;
        
        $periodoI = \Carbon\Carbon::parse($alumnos[0]->perFechaInicial)->format('d') . '/' . Utils::num_meses_corto_string(\Carbon\Carbon::parse($alumnos[0]->perFechaInicial)->format('m')) . '/' . \Carbon\Carbon::parse($alumnos[0]->perFechaInicial)->format('Y');
        $periodoF = \Carbon\Carbon::parse($alumnos[0]->perFechaFinal)->format('d') . '/' . Utils::num_meses_corto_string(\Carbon\Carbon::parse($alumnos[0]->perFechaFinal)->format('m')) . '/' . \Carbon\Carbon::parse($alumnos[0]->perFechaFinal)->format('Y');
        $periodoVigente = $periodoI . ' al ' . $periodoF . ' (' . $alumnos[0]->perNumero . '-' . $alumnos[0]->perAnio . ')';
    @endphp
    <header>
        <div class="row">
            <div class="columns medium-6">

                <h4 style="margin-top:0px; margin-bottom: 10px;">Preparatoria ESCUELA MODELO</h4>
                <h4 style="margin-top:0px; margin-bottom: 10px;">PUNTOS EVIDENCIAS</h4>
                <p><b>Ubicación:</b> {{ $alumnos[0]->ubiClave }}-{{ $alumnos[0]->ubiNombre }}</p>
                <p><b>Período:</b> {{ $periodoVigente }}</p>
                <p><b>Nivel:</b> {{ $alumnos[0]->progClave }} ({{ $alumnos[0]->planClave }})
                    {{ $alumnos[0]->progNombre }} {{ $alumnos[0]->gpoGrado }}° Grupo: {{ $alumnos[0]->gpoClave }} </p>
                <p><b>Docente:</b> {{ $alumnos[0]->empApellido1 }} {{ $alumnos[0]->empApellido2 }}
                    {{ $alumnos[0]->empNombre }}</p>
                <p><b>Materia:</b> {{ $alumnos[0]->matClave }}-{{ $alumnos[0]->matNombre }}</p>
                @if ($alumnos[0]->gpoMatComplementaria != '')
                    <p><b>Materia complementaria:</b> {{ $alumnos[0]->gpoMatComplementaria }}</p>
                @endif

            </div>
            <div class="columns medium-6">
                <div style="text-align: right;">
                    {{--  <p>{{$fechaActual}}</p>
            <p>{{$horaActual}}</p>  --}}
                    {{--  <p style="margin-top: 65px;">Mes: {{$mes_a_consultar}}</p>  --}}


                </div>
            </div>
        </div>
        {{--  <div class="row" style="margin-bottom: 2px;">
        <div class="columns medium-12">
          <p>
           Periodo:
          </p>
        </div>  --}}
        </div>
    </header>

    <footer id="footer">
        <div class="page-number"></div>
    </footer>

    @php
        $pos = 1;
        $pos2 = 1;
        $pos3 = 1;
        $pos4 = 1;
        $pos5 = 1;
        $pos6 = 1;
        $pos7 = 1;
        $pos8 = 1;
        $pos9 = 1;
        $count = 0;
        $periodo1 = 0;
        $periodo2 = 0;
        $periodo3 = 0;
        $sumaEvidencias = 0;
        $sumaEvidenciasReales = 0;
        
        $periodoAsignado1 = 0;
        $periodoAsignado2 = 0;
        $periodoAsignado3 = 0;
    @endphp

    <div class="row">
        <div class="columns medium-12">
            <table class="table">
                <thead>
                    <tr>
                        <td class="punteado" align="center" colspan="1" style="font-weight: 400;"></td>
                        <td class="punteado" align="center" colspan="1" style="font-weight: 400;"><b>Clave</b></td>
                        <td class="punteado" colspan="1" align="center" style="font-weight: 400;widtd: 350px;">
                            <b>Nombre del</b>
                        </td>

                        @if ($parametro_ubicacion == 'CVA')
                            <td class="punteado" colspan="{{ count($totalEvidecias) + 4 }}" align="center"
                                style="font-weight: 400; widtd: 20px;"><b>Número de evidencia(s)</b></td>
                        @endif
                    </tr>
                    <tr>
                        <td class="punteado3" align="center" style="font-weight: 400;"><b>#</b></td>
                        <td class="punteado3" align="center" style="font-weight: 400;"><b>Pago</b></td>
                        <td class="punteado3" align="center" style="font-weight: 400;widtd: 350px;"><b>Alumno</b></td>



                        @if ($parametro_ubicacion == 'CVA')
                            @foreach ($totalEvidecias as $evidencias => $totalEvideciasVal)
                                @foreach ($totalEvideciasVal as $ite)
                                    @if ($evidencias == $ite->eviNumero && $pos4++ == 1)
                                        @if (
                                            $bachiller_calendario_examen->calexInicioParcial1 <= $ite->eviFechaEntrega &&
                                                $bachiller_calendario_examen->calexFinParcial1 >= $ite->eviFechaEntrega)
                                            <td class="punteado2" align="center" style="font-weight: 400; widtd: 20px;">
                                                <b>{{ $evidencias }}</b>
                                                <br>
                                                <b>{{ number_format((float) $ite->eviPuntos, 1, '.', '') }}</b>

                                                @php
                                                    $periodoAsignado1 = $periodoAsignado1 + $ite->eviPuntos;
                                                @endphp
                                            </td>
                                        @endif
                                    @endif
                                @endforeach
                                @php
                                    $pos4 = 1;
                                @endphp
                            @endforeach
                        @endif

                        @if ($parametro_ubicacion == 'CVA')
                            <td class="punteado2" align="center" style="font-weight: 400; widtd: 20px;"><b>P1</b>
                                <br>
                                <b>{{ number_format((float) $periodoAsignado1, 1, '.', '') }}</b>
                            </td>
                        @endif

                        @if ($parametro_ubicacion == 'CVA')
                            @foreach ($totalEvidecias as $evidencias => $totalEvideciasVal)
                                @foreach ($totalEvideciasVal as $ite)
                                    @if ($evidencias == $ite->eviNumero && $pos5++ == 1)
                                        @if (
                                            $bachiller_calendario_examen->calexInicioParcial2 <= $ite->eviFechaEntrega &&
                                                $bachiller_calendario_examen->calexFinParcial2 >= $ite->eviFechaEntrega)
                                            <td class="punteado2" align="center" style="font-weight: 400; widtd: 20px;">
                                                <b>{{ $evidencias }}</b>
                                                <br>
                                                <b>{{ number_format((float) $ite->eviPuntos, 1, '.', '') }}</b>
                                                @php
                                                    $periodoAsignado2 = $periodoAsignado2 + $ite->eviPuntos;
                                                @endphp
                                            </td>
                                        @endif
                                    @endif
                                @endforeach
                                @php
                                    $pos5 = 1;
                                @endphp
                            @endforeach
                        @endif

                        @if ($parametro_ubicacion == 'CVA')
                            <td class="punteado2" align="center" style="font-weight: 400; widtd: 20px;"><b>P2</b>
                                <br>
                                <b>{{ number_format((float) $periodoAsignado2, 1, '.', '') }}</b>
                            </td>
                        @endif


                        @if ($parametro_ubicacion == 'CVA')
                            @foreach ($totalEvidecias as $evidencias => $totalEvideciasVal)
                                @foreach ($totalEvideciasVal as $ite)
                                    @if ($evidencias == $ite->eviNumero && $pos6++ == 1)
                                        @if (
                                            $bachiller_calendario_examen->calexInicioOrdinario <= $ite->eviFechaEntrega &&
                                                $bachiller_calendario_examen->calexFinOrdinario >= $ite->eviFechaEntrega)
                                            <td class="punteado2" align="center" style="font-weight: 400; widtd: 20px;">
                                                <b>{{ $evidencias }}</b>
                                                <br>
                                                <b>{{ number_format((float) $ite->eviPuntos, 1, '.', '') }}</b>
                                                @php
                                                    $periodoAsignado3 = $periodoAsignado3 + $ite->eviPuntos;
                                                @endphp
                                            </td>
                                        @endif
                                    @endif
                                @endforeach
                                @php
                                    $pos6 = 1;
                                @endphp
                            @endforeach
                        @endif

                        @if ($parametro_ubicacion == 'CVA')
                            <td class="punteado2" align="center" style="font-weight: 400; widtd: 20px;"><b>O</b>
                                <br>
                                <b>{{ number_format((float) $periodoAsignado3, 1, '.', '') }}</b>
                            </td>
                        @endif

                        <td class="punteado2" align="center"><b>Tot</b></td>

                    </tr>
                </thead>
                <tbody>

                    @if ($parametro_ubicacion == 'CVA')
                        @foreach ($totalEvideciasClave as $aluClave => $totalEvideciasClaveVal)
                            @foreach ($totalEvideciasClaveVal as $item)
                                @if ($aluClave == $item->aluClave && $pos2++ == 1)
                                    <tr>
                                        <td style="height: 30px;" class="punteado2" align="center">{{ $pos++ }}
                                        </td>
                                        <td class="punteado2" align="center">{{ $item->aluClave }}</td>
                                        <td class="punteado2" align="">{{ $item->perApellido1 }}
                                            {{ $item->perApellido2 }} {{ $item->perNombre }}</td>
                                        @foreach ($totalEvidecias as $evidencias => $totalEvideciasVal)
                                            @foreach ($totalEvideciasVal as $itemEv)
                                                @if ($evidencias == $itemEv->eviNumero && $pos3++ == 1 && $itemEv->aluClave == $aluClave)
                                                    @if (
                                                        $bachiller_calendario_examen->calexInicioParcial1 <= $itemEv->eviFechaEntrega &&
                                                            $bachiller_calendario_examen->calexFinParcial1 >= $itemEv->eviFechaEntrega)
                                                        <td class="punteado2" align="center"
                                                            style="font-weight: 400; widtd: 20px;">
                                                            {{ $itemEv->ievPuntos }}</td>
                                                        @php
                                                            $periodo1 = $periodo1 + $itemEv->ievPuntos;
                                                        @endphp
                                                    @endif
                                                @endif
                                                @php
                                                    $pos3 = 1;
                                                @endphp
                                            @endforeach
                                        @endforeach
                                        <td class="punteado2" align="center"><b>{{ $periodo1 }}</b></td>


                                        @foreach ($totalEvidecias as $evidencias => $totalEvideciasVal)
                                            @foreach ($totalEvideciasVal as $itemEv)
                                                @if ($evidencias == $itemEv->eviNumero && $pos6++ == 1 && $itemEv->aluClave == $aluClave)
                                                    @if (
                                                        $bachiller_calendario_examen->calexInicioParcial2 <= $itemEv->eviFechaEntrega &&
                                                            $bachiller_calendario_examen->calexFinParcial2 >= $itemEv->eviFechaEntrega)
                                                        <td class="punteado2" align="center"
                                                            style="font-weight: 400; widtd: 20px;">
                                                            {{ $itemEv->ievPuntos }}</td>
                                                        @php
                                                            $periodo2 = $periodo2 + $itemEv->ievPuntos;
                                                        @endphp
                                                    @endif
                                                @endif
                                                @php
                                                    $pos6 = 1;
                                                @endphp
                                            @endforeach
                                        @endforeach

                                        <td class="punteado2" align="center"><b>{{ $periodo2 }}</b></td>




                                        {{--  periodo 3   --}}
                                        @foreach ($totalEvidecias as $evidencias => $totalEvideciasVal)
                                            @foreach ($totalEvideciasVal as $itemEv)
                                                @if ($evidencias == $itemEv->eviNumero && $pos7++ == 1 && $itemEv->aluClave == $aluClave)
                                                    @if (
                                                        $bachiller_calendario_examen->calexInicioOrdinario <= $itemEv->eviFechaEntrega &&
                                                            $bachiller_calendario_examen->calexFinOrdinario >= $itemEv->eviFechaEntrega)
                                                        <td class="punteado2" align="center"
                                                            style="font-weight: 400; widtd: 20px;">
                                                            {{ $itemEv->ievPuntos }}</td>
                                                        @php
                                                            $periodo3 = $periodo3 + $itemEv->ievPuntos;
                                                        @endphp
                                                    @endif
                                                @endif
                                                @php
                                                    $pos7 = 1;
                                                @endphp
                                            @endforeach
                                        @endforeach

                                        <td class="punteado2" align="center"><b>{{ $periodo3 }}</b></td>

                                        <td class="punteado2" align="center">
                                            <b>{{ $periodo1 + $periodo2 + $periodo3 }}</b>
                                        </td>



                                        @php
                                            $periodo1 = 0;
                                            $periodo2 = 0;
                                            $periodo3 = 0;
                                        @endphp
                                    </tr>
                                @endif
                            @endforeach
                            @php
                                $pos2 = 1;
                            @endphp
                        @endforeach
                    @endif
                </tbody>
            </table>
        </div>
    </div>


</body>

</html>
